# BLE Mesh Network – Python Dependencies
# Install with: pip install -r requirements.txt

# BLE central role (scanning, connecting, writing to peers)
bleak>=0.21.1

# BLE peripheral role (advertising, GATT server)
bless>=0.2.6

# AES-GCM encryption + X25519 ECDH key exchange
cryptography>=42.0.0

# Async utilities (included in Python 3.9+ stdlib, listed for clarity)
# asyncio  – stdlib

# Optional: structured logging
# structlog>=24.0.0